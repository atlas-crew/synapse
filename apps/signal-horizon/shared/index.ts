/**
 * Signal Horizon Shared Module
 *
 * This module provides shared types and defaults for the Signal Horizon API and UI.
 *
 * @example
 * // Import types
 * import type { DlpConfig, TarpitConfig } from '@signal-horizon/shared/types';
 *
 * // Import defaults
 * import { DEFAULT_DLP_ENABLED } from '@signal-horizon/shared/defaults';
 */

export * from './types';
export * from './defaults';
